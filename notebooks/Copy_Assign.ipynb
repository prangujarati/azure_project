dbutils.widgets.text(
    name="package_path",
    defaultValue="/dbfs/FileStore/packages/default_package.whl",  # Default .whl path
    label="Enter the path to the .whl file:"
)

package_path = dbutils.widgets.get("package_path")
%pip install --force-reinstall $package_path 
